# Packaged Addon Services

This directory is reserved for the gradual migration of addon services out of
the single-file `addon.py`. Phase 7B creates the package layout and build path
only; current runtime compatibility remains anchored in `addon.py`.
